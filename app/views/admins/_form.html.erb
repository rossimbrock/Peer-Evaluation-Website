<!-- Owner: David Paquin !-->
<!-- Date: 11/29/2022 !-->
<%= form_with(model: admin) do |form| %>
  <% if admin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
        <% admin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
  First name: <%= form.text_field :first_name %><br>
  Last name: <%= form.text_field :last_name %><br>
  OSU name.#: <%= form.text_field :namedotnumber %><br>
  <%= form.submit "Save"%>
  </div>
<% end %>
